# For Local Development:
VITE_API_URL=http://localhost:5001/api

# For Production (uncomment and use when deploying):
# VITE_API_URL=https://your-backend-app.onrender.com/api

# Notes:
# 1. Copy this file to .env: cp .env.example .env
# 2. For local development, use the localhost URL above
# 3. For production deployment to Vercel, create .env.production with your Render URL
# 4. All Vite environment variables must start with VITE_
# 5. Never commit .env file to Git (it's in .gitignore)